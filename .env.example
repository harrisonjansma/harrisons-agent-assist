# Copy to .env and fill in. Real values are secrets — never commit .env.
# See DEVELOPMENT_SPEC.md §3 for where each value comes from.

# --- apps/worker (server-side only) ---
DEEPGRAM_API_KEY=          # Deepgram console (stored in BWS vault)
OPENAI_API_KEY=            # OpenAI dashboard (stored in BWS vault)
SUPABASE_URL=https://ntvwaczcgvzfdvykqnwo.supabase.co
SUPABASE_SERVICE_ROLE_KEY= # Supabase → Project Settings → API (service_role)
PORT=8080                  # injected by Railway in prod

# --- apps/web (public-safe) ---
NEXT_PUBLIC_WS_URL=wss://ws.agentassistdemo.harrisonjansma.com/ws
NEXT_PUBLIC_SUPABASE_URL=https://ntvwaczcgvzfdvykqnwo.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY= # Supabase → Project Settings → API (anon)
